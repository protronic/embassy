// -------------------------------------------------- //
// This file is autogenerated by pioasm; do not edit! //
// -------------------------------------------------- //

#pragma once

#if !PICO_NO_HARDWARE
#include "hardware/pio.h"
#endif

// ----- //
// hsync //
// ----- //

#define hsync_wrap_target 1
#define hsync_wrap 16
#define hsync_pio_version 1

static const uint16_t hsync_program_instructions[] = {
    0x80a0, //  0: pull   block
            //     .wrap_target
    0xf424, //  1: set    x, 4            side 1
    0xbc42, //  2: nop                    side 3
    0x1442, //  3: jmp    x--, 2          side 1
    0xbc42, //  4: nop                    side 3
    0xb427, //  5: mov    x, osr          side 1
    0xbc42, //  6: nop                    side 3
    0x1446, //  7: jmp    x--, 6          side 1
    0xf834, //  8: set    x, 20           side 2
    0xb042, //  9: nop                    side 0
    0xb842, // 10: nop                    side 2
    0xb042, // 11: nop                    side 0
    0x1849, // 12: jmp    x--, 9          side 2
    0xf43f, // 13: set    x, 31           side 1
    0xbc42, // 14: nop                    side 3
    0x144e, // 15: jmp    x--, 14         side 1
    0xdc00, // 16: irq    nowait 0        side 3
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program hsync_program = {
    .instructions = hsync_program_instructions,
    .length = 17,
    .origin = -1,
    .pio_version = hsync_pio_version,
#if PICO_PIO_VERSION > 0
    .used_gpio_ranges = 0x0
#endif
};

static inline pio_sm_config hsync_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + hsync_wrap_target, offset + hsync_wrap);
    sm_config_set_sideset(&c, 3, true, false);
    return c;
}

static inline void hsync_program_init(PIO pio, uint sm, uint offset, uint pin, float div) {
    // creates state machine configuration object c, sets
    // to default configurations. I believe this function is auto-generated
    // and gets a name of <program name>_program_get_default_config
    pio_sm_config c = hsync_program_get_default_config(offset);
    // Map the state machine's SET pin group to one pin, namely the `pin`
    // parameter to this function.
    sm_config_set_sideset_pins(&c, pin);
    // Set clock division (div by 5 for 25 MHz state machine)
    sm_config_set_clkdiv(&c, div) ;
    // Set this pin's GPIO function (connect PIO to the pad)
    pio_gpio_init(pio, pin);
    pio_gpio_init(pio, pin+1);
    // Set the pin direction to output at the PIO
    pio_sm_set_consecutive_pindirs(pio, sm, pin, 2, true);
    // Load our configuration, and jump to the start of the program
    pio_sm_init(pio, sm, offset, &c);
    // Set the state machine running (commented out so can be synchronized w/ vsync)
    // pio_sm_set_enabled(pio, sm, true);
}

#endif

// ----- //
// vsync //
// ----- //

#define vsync_wrap_target 1
#define vsync_wrap 10
#define vsync_pio_version 1

static const uint16_t vsync_program_instructions[] = {
    0x80a0, //  0: pull   block
            //     .wrap_target
    0xa027, //  1: mov    x, osr
    0x20c0, //  2: wait   1 irq, 0
    0xc001, //  3: irq    nowait 1
    0x0042, //  4: jmp    x--, 2
    0x20c0, //  5: wait   1 irq, 0
    0xe000, //  6: set    pins, 0
    0x20c0, //  7: wait   1 irq, 0
    0xe05f, //  8: set    y, 31
    0x38c0, //  9: wait   1 irq, 0        side 1
    0x0089, // 10: jmp    y--, 9
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program vsync_program = {
    .instructions = vsync_program_instructions,
    .length = 11,
    .origin = -1,
    .pio_version = vsync_pio_version,
#if PICO_PIO_VERSION > 0
    .used_gpio_ranges = 0x0
#endif
};

static inline pio_sm_config vsync_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + vsync_wrap_target, offset + vsync_wrap);
    sm_config_set_sideset(&c, 2, true, false);
    return c;
}

static inline void vsync_program_init(PIO pio, uint sm, uint offset, uint pin, float div) {
    // creates state machine configuration object c, sets
    // to default configurations. I believe this function is auto-generated
    // and gets a name of <program name>_program_get_default_config
    pio_sm_config c = vsync_program_get_default_config(offset);
    // Map the state machine's SET pin group to one pin, namely the `pin`
    // parameter to this function.
    sm_config_set_set_pins(&c, pin, 1);
    sm_config_set_sideset_pins(&c, pin);
    // Set clock division (div by 5 for 25 MHz state machine)
    sm_config_set_clkdiv(&c, div) ;
    // Set this pin's GPIO function (connect PIO to the pad)
    pio_gpio_init(pio, pin);
    // pio_gpio_init(pio, pin+1);
    // Set the pin direction to output at the PIO
    pio_sm_set_consecutive_pindirs(pio, sm, pin, 1, true);
    // Load our configuration, and jump to the start of the program
    pio_sm_init(pio, sm, offset, &c);
    // Set the state machine running (commented out so can be synchronized with hsync)
    // pio_sm_set_enabled(pio, sm, true);
}

#endif

// --- //
// rgb //
// --- //

#define rgb_wrap_target 3
#define rgb_wrap 9
#define rgb_pio_version 1

static const uint16_t rgb_program_instructions[] = {
    0x80a0, //  0: pull   block
    0xa047, //  1: mov    y, osr
    0x20a3, //  2: wait   1 pin, 3
            //     .wrap_target
    0xa022, //  3: mov    x, y
    0x20a2, //  4: wait   1 pin, 2
    0x80a0, //  5: pull   block
    0x2025, //  6: wait   0 pin, 5
    0x6010, //  7: out    pins, 16
    0x20a5, //  8: wait   1 pin, 5
    0x0045, //  9: jmp    x--, 5
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program rgb_program = {
    .instructions = rgb_program_instructions,
    .length = 10,
    .origin = -1,
    .pio_version = rgb_pio_version,
#if PICO_PIO_VERSION > 0
    .used_gpio_ranges = 0x0
#endif
};

static inline pio_sm_config rgb_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + rgb_wrap_target, offset + rgb_wrap);
    return c;
}

static inline void rgb_program_init(PIO pio, uint sm, uint offset, uint pin, float div) {
    // creates state machine configuration object c, sets
    // to default configurations. I believe this function is auto-generated
    // and gets a name of <program name>_program_get_default_config
    pio_sm_config c = rgb_program_get_default_config(offset);
    // Map the state machine's SET and OUT pin group to 16 pins, the `pin`
    // parameter to this function is the lowest one. These groups overlap.
    sm_config_set_set_pins(&c, pin, 16);
    sm_config_set_out_pins(&c, pin, 16);
    // Set clock division (Commented out, this one runs at full speed)
    sm_config_set_clkdiv(&c, div) ;
    // Set this pin's GPIO function (connect PIO to the pad)
    pio_gpio_init(pio, pin);    
    pio_gpio_init(pio, pin+1);
    pio_gpio_init(pio, pin+2);
    pio_gpio_init(pio, pin+3);    
    pio_gpio_init(pio, pin+4);
    pio_gpio_init(pio, pin+5);
    pio_gpio_init(pio, pin+6);
    pio_gpio_init(pio, pin+7);
    pio_gpio_init(pio, pin+8);
    pio_gpio_init(pio, pin+9);
    pio_gpio_init(pio, pin+10);
    pio_gpio_init(pio, pin+11);
    pio_gpio_init(pio, pin+12);
    pio_gpio_init(pio, pin+13);
    pio_gpio_init(pio, pin+14);
    pio_gpio_init(pio, pin+15);
    // Set the pin direction to output at the PIO (4 pins)
    pio_sm_set_consecutive_pindirs(pio, sm, pin, 16, true);
    pio_set_irq0_source_enabled(pio, (enum pio_interrupt_source) ((uint) pis_interrupt0 + sm), false);
    pio_set_irq1_source_enabled(pio, (enum pio_interrupt_source) ((uint) pis_interrupt0 + sm), false);
    pio_interrupt_clear(pio, sm);
    // Load our configuration, and jump to the start of the program
    pio_sm_init(pio, sm, offset, &c);
    // Set the state machine running (commented out, I'll start this in the C)
    // pio_sm_set_enabled(pio, sm, true);
}

#endif

// -------- //
// RGBframe //
// -------- //

#define RGBframe_wrap_target 1
#define RGBframe_wrap 3
#define RGBframe_pio_version 1

static const uint16_t RGBframe_program_instructions[] = {
    0x80a0, //  0: pull   block
            //     .wrap_target
    0xa047, //  1: mov    y, osr
    0x30c1, //  2: wait   1 irq, 1        side 0
    0x1883, //  3: jmp    y--, 3          side 1
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program RGBframe_program = {
    .instructions = RGBframe_program_instructions,
    .length = 4,
    .origin = -1,
    .pio_version = RGBframe_pio_version,
#if PICO_PIO_VERSION > 0
    .used_gpio_ranges = 0x0
#endif
};

static inline pio_sm_config RGBframe_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + RGBframe_wrap_target, offset + RGBframe_wrap);
    sm_config_set_sideset(&c, 2, true, false);
    return c;
}

// Helper function (for use in C program) to initialize this PIO program
void RGBframe_program_init(PIO pio, uint sm, uint offset, uint pin, float div) {
    // Sets up state machine and wrap target. This function is automatically
    // generated in RGBframe.pio.h.
    pio_sm_config c = RGBframe_program_get_default_config(offset);
    // Allow PIO to control GPIO pin (as output)
    sm_config_set_set_pins(&c, pin, 1);
    sm_config_set_sideset_pins(&c, pin);
    // Connect pin to SET pin (control with 'set' instruction)
    pio_gpio_init(pio, pin);
    // Set the pin direction to output (in PIO)
    pio_sm_set_consecutive_pindirs(pio, sm, pin, 1, true);
    // Set the clock divider for the state machine
    sm_config_set_clkdiv(&c, div);
    // Load configuration and jump to start of the program
    pio_sm_init(pio, sm, offset, &c);
}

#endif

